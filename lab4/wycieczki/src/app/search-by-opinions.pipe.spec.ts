import { SearchByOpinionsPipe } from './search-by-opinions.pipe';

describe('SearchByOpinionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByOpinionsPipe();
    expect(pipe).toBeTruthy();
  });
});
