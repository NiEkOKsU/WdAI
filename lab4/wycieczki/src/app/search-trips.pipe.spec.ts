import { SearchTripsPipe } from './search-trips.pipe';

describe('SearchTripsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTripsPipe();
    expect(pipe).toBeTruthy();
  });
});
